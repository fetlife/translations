test:
  override:
    - bundle exec i18n-tasks missing

deployment:
  production:
    branch: master
    commands:
      - ./scripts/deploy.sh

dependencies:
  post:
    - gem install yaml-lint

notify:
  webhooks:
    - url: https://fetlife-johnny5.herokuapp.com/circleci

test:
 override:
  - yaml-lint locales
